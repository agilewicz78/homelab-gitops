apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: helpdesk-postgres-data
  namespace: helpdesk
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 10Gi
