apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nginx-demo-lab-local
  namespace: nginx-demo
spec:
  secretName: nginx-demo-lab-local-tls
  dnsNames:
    - nginx-demo.lab.local
  issuerRef:
    name: lab-local-ca-issuer
    kind: ClusterIssuer
