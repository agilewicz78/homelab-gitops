apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tools-lab-local
  namespace: tools
spec:
  secretName: tools-lab-local-tls
  dnsNames:
    - tools.lab.local
  issuerRef:
    name: lab-local-ca-issuer
    kind: ClusterIssuer
